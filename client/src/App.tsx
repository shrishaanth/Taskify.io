/**
 * App shell placeholder. Real routing/layout arrives in Phase 4.
 * For now it only proves the design tokens load and apply.
 */
export default function App() {
  return (
    <main style={{ padding: "var(--space-8)" }}>
      <h1>Taskify</h1>
      <p style={{ color: "var(--text-secondary)" }}>
        Design tokens loaded. UI is built in later phases.
      </p>
    </main>
  );
}

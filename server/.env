PORT=5000
DATABASE_URL=mongodb+srv://shrishaanth2024_db_user:Shri06@cluster0.vqhuqth.mongodb.net/?appName=Cluster0
JWT_SECRET=taskify_super_secret_jwt_key_2024
JWT_EXPIRES_IN=7d
NODE_ENV=development

# Optional: enables multi-instance sockets, caching, presence, job locks
# REDIS_URL=redis://localhost:6379

# Optional: extra comma-separated CORS origins for deployed clients
# CLIENT_ORIGINS=https://your-client.example.com

# Optional: instance label surfaced by /api/health (set per replica)
# INSTANCE_ID=app-1
